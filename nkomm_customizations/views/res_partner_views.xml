<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">Nkomm Partner Form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group/group[2]" position="inside">
                <field name="digital_invoice" widget="boolean_toggle" help="Möchte der Kunde Rechnungen digital erhalten?"/>
            </xpath>
            <xpath expr="//span[@name='address_name']" position="before">
                <field name="sequence_number" string="Customer Number"/>
            </xpath>
            <xpath expr="//field[@name='title']" position="replace"/>
            <xpath expr="//field[@id='individual']" position="attributes">
                <attribute name="style">display:none</attribute>
            </xpath>
            <xpath expr="//field[@name='name']" position="before">
                <field name="title" options="{&quot;no_open&quot;: True}" placeholder="e.g. Mister" attrs="{'invisible': [('is_company', '=', True)]}"/>
                <span attrs="{'invisible': [('is_company','=', True)]}">.</span>
                <field attrs="{'invisible': [('is_company','=', True)]}" class="ml-1" name="first_name" placeholder="Vorname"/>
                <field attrs="{'invisible': [('is_company','=', True)]}" class="ml-2" name="last_name" placeholder="Nachname"/>
            </xpath>
        </field>
    </record>

    <record id="view_partner_tree" model="ir.ui.view">
        <field name="name">Nkomm Partner Tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='display_name']" position="before">
                <field name="sequence_number" string="Customer Number" optional="hide"/>
            </xpath>
        </field>
    </record>
</odoo>

