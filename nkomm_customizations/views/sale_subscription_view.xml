<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="sale_subscription_view_form" model="ir.ui.view">
        <field name="name">NKomm Sale Subscription Form</field>
        <field name="model">sale.subscription</field>
        <field name="inherit_id" ref="sale_subscription.sale_subscription_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="first_invoice" string="First Invoice Done?" invisible="1"/>
                <field name="start_subscription_on"/>
                <field name="end_subscription_on"/>
            </xpath>

        </field>
    </record>
</odoo>
